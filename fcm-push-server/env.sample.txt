# Render Environment Variables

FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
FCM_PUSH_API_KEY=your-secret-key
FCM_PROJECT_ID=mota7-go

# اختياري: معالجة spark_fcm_jobs كل 30 ثانية
FCM_JOBS_POLL_MS=30000

# اختياري: تفعيل وضع الاختبار عبر env بدل config (1 = on)
# FCM_TEST_OVERRIDE=1

# وضع الاختبار الافتراضي في config/recipient-criteria.cjs:
# testOverride.enabled + providerPhones — عطّله قبل الإنتاج
