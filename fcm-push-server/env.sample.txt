# Render / local — FCM لجميع طلبات الخدمة: delivery | education | other
# انسخ إلى Environment Variables في لوحة Render (Web Service)

# JSON كامل لحساب خدمة Firebase (مشروع mota7-go) — صلاحية Firestore + FCM
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# مفتاح سري يطابق التطبيق (environment.fcmPushApiKey) وطلبات cron
FCM_PUSH_API_KEY=change-me-long-random-secret

# اختياري: معالجة spark_fcm_jobs كل N مللي ثانية (مثلاً 30000 = 30 ثانية)
# احتياطي إذا تأخر POST من التطبيق أو توقف الخدمة مؤقتاً (خطة Render المجانية)
FCM_JOBS_POLL_MS=30000

# Render يضبط PORT تلقائياً — لا تغيّره يدوياً
