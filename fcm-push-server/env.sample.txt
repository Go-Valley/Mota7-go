# Render Environment Variables (mota7-fcm-push-server)

# مطلوب
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
FCM_PUSH_API_KEY=your-secret-key
FCM_PROJECT_ID=mota7-go

# موصى به: معالجة spark_fcm_jobs كل 30 ثانية (احتياط إذا فشل POST المباشر)
FCM_JOBS_POLL_MS=30000

# ─── وضع الإنتاج (إرسال لكل مقدمي الخدمة المطابقين) ───
# لا تضف FCM_TEST_OVERRIDE، أو احذفها من لوحة Render إن وُجدت.
# بعد النشر تحقق: GET https://mota7-fcm-push-server.onrender.com/
# يجب أن يظهر: "testOverride": false

# ─── وضع الاختبار (3 أرقام في recipient-criteria.cjs) ───
# FCM_TEST_OVERRIDE=1
# يجب أن يكون لكل رقم اختبار:
#   1) حساب مقدم خدمة {phone}@mota7.com
#   2) مستند users/{phone} مع نفس uid
#   3) device_tokens مع owner_phone و app=mota7 (يفتح التطبيق ويمنح إذن الإشعارات)

# ntfy — نشر طلبات الخدمة من الخادم (نفس موضوع التطبيق)
NTFY_BASE_URL=https://ntfy.sh
NTFY_ORDERS_TOPIC=mota7-go-ads-change-this-secret-topic
NTFY_ORDERS_ENABLED=1
