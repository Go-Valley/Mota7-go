# Copy these as environment variables on Render / Railway / etc.

FIREBASE_PROJECT_ID=mota7-go

CLOUDINARY_CLOUD_NAME=dizjsif73
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Render sets PORT automatically; locally default is 8787

# --- After deploy: set cloudinaryDeleteProxyUrl in Mota7 + mota7-admin environment.prod.ts
# to your public URL (https://...), no trailing slash, no /delete.

# --- Local + Android
# Web (ng serve): http://127.0.0.1:8787  (already in environment.ts)
# Android emulator build: http://10.0.2.2:8787  (environment.capacitor.ts via ng build --configuration android)
# Physical phone on same Wi-Fi: edit environment.capacitor.ts to http://YOUR_PC_LAN_IP:8787
